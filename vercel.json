{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "set -ux; cd artifacts/hcn-studio && pnpm run build && cd ../.. && echo '=== post-build state ===' && pwd && ls -la artifacts/hcn-studio/ && rm -rf dist && cp -R artifacts/hcn-studio/dist dist && ls -la dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://lunastory-api-741799167288.asia-northeast3.run.app/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
